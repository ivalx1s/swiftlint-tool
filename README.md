```bash
USAGE: swift-lint-tool <subcommand>

OPTIONS:
  -h, --help              Show help information.

SUBCOMMANDS:
  precommit (default)     Run SwiftLint for pre-commit hook.
  prebuild                Run SwiftLint for pre-build script.
```
